from setuptools import setup

setup(name='ensemble',
      version='0.1',
      description='Package for ensembling Machine Learning models.',
      url='https://github.com/pklauke/Ensemble',
      author='Peter Klauke',
      packages=['ensemble'],
      zip_safe=False)